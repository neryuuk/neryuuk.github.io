---
layout: default
---
{% assign data = site.data[page.lang] %}
<!DOCTYPE html>
<html lang="{{ page.lang }}">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>{{ data.header.name }} - {{ data.header.title }}</title>
    {% include seo.liquid data=data.header %}
  </head>
  <body>
    <div id="body">
      <div id="main-column">
        <code>{{ data }}</code>
      </div>
    </div>
  </body>
</html>
