<header>
  <div id="name-title">
    <h1 id="name">{{ data.header.name }}</h1>
    <h3 id="title">{{ data.header.title }}</h3>
  </div>
  <div>
    <div>
      {% for each in data.details %}{% include span.liquid data=each %}{% endfor %}
    </div>
    <div>
      {% for each in data.links %}{% include span.liquid data=each %}{% endfor %}
    </div>
  </div>
</header>
